{
  "name": "career-profile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "validate:source": "node scripts/validate-public-source.mjs",
    "validate:release": "node scripts/validate-release.mjs",
    "check:release": "npm run validate:source && npm run build && npm run validate:release",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "7.3.2"
  }
}
